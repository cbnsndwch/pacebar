export const appName = "PaceBar";
export const docsRoute = "/docs";

export const gitConfig = {
  user: "cbnsndwch",
  repo: "pacebar",
  branch: "main",
};

export const githubUrl = `https://github.com/${gitConfig.user}/${gitConfig.repo}`;
export const releasesUrl = `${githubUrl}/releases/latest`;
